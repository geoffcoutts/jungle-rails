You have successfully placed an order at Jungle!

Your order is as follows:

*************************

<% @order.line_items.each do | line_item | %>

  <%= line_item.product.name %>
  <%= line_item.product.description %>
  <%= humanized_money_with_symbol line_item.product.price %>
  <%= line_item.quantity %>
  <%= humanized_money_with_symbol (line_item.product.price * line_item.quantity) %>
<% end %>

<%= humanized_money_with_symbol (order.total_cents * 0.01) %>

Thanks for shopping and have a great day!