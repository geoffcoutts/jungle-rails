<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank You For Your Order, <%= @user.name %></h1>
    <p>
      You have successfully placed an order at Jungle.<br>
    </p>
    <p>
      Your order is as follows:
    </p>
    <table>
      <thead>
        <tr>
          <th>Product</th>
          <th>Unit Price</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% order.line_items.each do | line_item | %>
          <tr>
            <td>
              <%= image_tag product.image.tiny %>
            </td>
            <td>
              <h4><%= line_item.product.name %></h4>
              <br>
              <p><%= line_item.product.description %></p>
            </td>
            <td>
              <%= line_item.product.price %>
            </td>
            <td>
              <%= line_item.quantity %>
            </td>
            <td>
              <%= line_item.product.price * line_item.quantity %>
            </td>
          </tr>
        <% end %>
      </tbody>
      <tfoot>
        <th>
          <%= number_to_currency(@order.total_cents * 0.01) %>
        </th>
      </tfoot>
    </table>
    <p>Thanks for shopping and have a great day!</p>
  </body>
</html>